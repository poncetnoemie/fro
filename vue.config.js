module.exports = {
  // github pages url
  // publicPath: process.env.NODE_ENV === "production" ? "/fro/" : "/"
  // custom domain :
  publicPath: process.env.NODE_ENV === "production" ? "./" : "/"
};
